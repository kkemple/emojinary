machine:
  node:
    version: 0.12.0

deployment:
  elasticbeanstalk:
    branch: master
    commands:
      - sudo bash ./bin/circle-ci-aws-creds.sh
      - pip install awsebcli
      - eb deploy emojinary -l $CIRCLE_SHA1
