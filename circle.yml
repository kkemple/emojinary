machine:
  node:
    version: 0.12.0

deployment:
  elasticbeanstalk:
    branch: master
    commands:
      - bash ./bin/circle-ci-aws-creds.sh
      - pip install awsebcli
      - eb init emojinary -r us-west-2 -p Docker
      - eb deploy emojinary -l $CIRCLE_SHA1
